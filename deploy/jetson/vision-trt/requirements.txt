# TensorRT + CUDA + OpenCV: installed via apt (python3-libnvinfer, python3-opencv)
# PyCUDA: installed separately in Dockerfile

# Networking
pyzmq
msgpack

# Web server
uvicorn[standard]
fastapi
websockets
python-multipart

# Utilities
Pillow
